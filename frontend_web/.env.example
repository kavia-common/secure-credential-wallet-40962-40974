# Frontend (Flutter) Environment Variables

# Base URL for Backend API (FastAPI)
# Example: http://localhost:8000 or the dev proxy URL.
BACKEND_BASE_URL=http://localhost:8000

# Feature flags (comma-separated list, use for conditional UI/flows)
FEATURE_FLAGS=
